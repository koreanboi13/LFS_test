go:
  version: "1.22"
  race: true
  mod_readonly: true
  proxy: "https://proxy.golang.org,direct"

steps:
  lint: true
  test: true
  vuln: true
  build: true

lint:
  timeout: "5m"
  config: ".golangci.yml"

test:
  packages: "./..."
  coverage: true

vuln:
  packages: "./..."
