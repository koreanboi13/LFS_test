run:
  timeout: 5m

linters:
  enable:
    - gosec
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - bodyclose
    - gocritic

linters-settings:
  gosec:
    severity: medium
    confidence: medium

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
